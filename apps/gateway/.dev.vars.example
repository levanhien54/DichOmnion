# apps/gateway/.dev.vars — MẪU (template). Copy file này → `.dev.vars` để chạy `wrangler dev`
# cục bộ. `.dev.vars` đã bị .gitignore (dòng 24), file `.dev.vars.example` này thì KHÔNG bị
# ignore nên được commit làm khuôn. Đây LÀ giá-trị PLACEHOLDER — TUYỆT ĐỐI không dán khoá thật
# vào file này rồi commit.
#
# Định dạng: dotenv (wrangler đọc `.dev.vars` kiểu dotenv — `KEY=value`, một biến mỗi dòng).
# Chỉ CẦN cho `pnpm run dev` (wrangler dev, cổng 8787). KHÔNG cần cho `pnpm run test` (vitest tự
# inject key trong test). Boot vẫn chạy nếu thiếu, nhưng mọi đường có xác-thực sẽ FAIL-CLOSED
# (`gateway_key_missing`) cho tới khi có `GATEWAY_JWT_PRIVATE_KEY`.
# PRODUCTION KHÔNG dùng file này — nạp secret bằng `wrangler secret put <TÊN>` (docs/DEPLOYMENT.md §2.6).

# Khoá RIÊNG ES256 (P-256, PKCS8 PEM) Gateway dùng KÝ JWT mỗi job (Trạm 2). Worker chỉ giữ khoá
# CÔNG KHAI tương ứng. Sinh cả cặp bằng helper (in sẵn lệnh nạp cho mỗi bên):
#   scripts/gen-gateway-keys.sh
# Giữ xuống-dòng THẬT của PEM, bọc trong nháy kép:
GATEWAY_JWT_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----
<dán PKCS8 PEM khoá RIÊNG (dev) vào đây>
-----END PRIVATE KEY-----"

# Secret Cloudflare Turnstile (đi cặp với VITE_TURNSTILE_SITE_KEY của Client) — chống bot khi đăng ký.
TURNSTILE_SECRET="<Turnstile secret key>"

# Tách secret theo quyền: watchdog chỉ được điều khiển Financial Kill Switch; RunPod controller
# chỉ được xin lease/publish/clear worker target. Không nhập hai giá trị này vào ứng dụng desktop.
ADMIN_TOKEN="<shared secret cho kill-switch watchdog>"
WORKER_TARGET_ADMIN_TOKEN="<shared secret riêng cho RunPod controller>"

# Static GPU Worker fallback. A live SDK-controller heartbeat takes precedence. Plain HTTP is
# accepted only for loopback development; every remote URL must use HTTPS and no credentials.
WORKER_URL="http://127.0.0.1:8000"
WORKER_CAPABILITY_TIMEOUT_MS="3500"
WORKER_CAPABILITY_CACHE_MS="1000"
# RunPod proxy deployments must keep this at or below 90000 (90 seconds).
MAX_RENDER_MS="90000"
# Long ANALYZE jobs use short authenticated submit/status requests. Enable only
# after the deployed worker image exposes /api/worker/analyze/submit and /status.
ASYNC_ANALYZE_ENABLED="true"
ASYNC_ANALYZE_TIMEOUT_MS="900000"
ASYNC_ANALYZE_POLL_MS="2500"

# --- R2 (object storage cho AUDIO) — cho POST /api/uploads/presign ------------------
# Gateway KÝ presigned R2 URL mỗi job (Option A). Client gọi /api/uploads/presign, Gateway
# ký một cặp PUT/GET (SigV4, WebCrypto) rồi trả về — Gateway CHỈ ký, KHÔNG trung chuyển bytes.
# Thiếu bất kỳ biến BẮT BUỘC nào bên dưới → endpoint FAIL-CLOSED 503 (không bịa URL giả).
# PRODUCTION: nạp bằng `wrangler secret put <TÊN>` (ACCESS_KEY/SECRET là BÍ MẬT — đừng commit).
#   R2 → S3 API token: Cloudflare Dashboard → R2 → Manage R2 API Tokens → tạo token có
#   quyền Object Read & Write trên bucket; lấy Access Key ID 32 ký tự + Secret Access Key
#   64 ký tự. Không dùng nhầm Cloudflare API Token làm S3 Access Key ID.
R2_ACCOUNT_ID="385e2b411beb41a79d6b45477bc3f544"   # mã tài khoản Cloudflare → host <id>.r2.cloudflarestorage.com
R2_BUCKET="sonsonjh"                                # tên bucket R2 chứa audio job
R2_ACCESS_KEY_ID="<R2 S3 Access Key ID>"            # BÍ MẬT (dev có thể để placeholder)
R2_SECRET_ACCESS_KEY="<R2 S3 Secret Access Key>"    # BÍ MẬT — TUYỆT ĐỐI không commit khoá thật
# Tuỳ chọn (có mặc định an toàn trong code):
#   R2_REGION           — mặc định 'auto' (R2). Chỉ đổi nếu nhà cung cấp yêu cầu.
#   R2_PRESIGN_EXPIRES_S — hạn sống của URL ký (giây), mặc định 7200 (2h), tối đa 604800.
#   PRESIGN_RATE_LIMIT   — số lần ký tối đa mỗi thiết bị trong cửa sổ, mặc định 60.
#   PRESIGN_WINDOW_S     — độ dài cửa sổ throttle (giây), mặc định 3600.
